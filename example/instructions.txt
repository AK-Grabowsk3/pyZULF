# Raising oscillation
x <- sin_strange( 1.0, amp*0.6666/3 )
x <- sin_strange( skew, amp/3 )
x:y/10 <- [T = 20] sin_strange( skew, amp ) 


# rising field
x <- [T = 10] line( 0.0, 2.0 )
x <- [T = 10] 2

t_rot := current_time()

# rotational field
x <- [T = 4.] sin( 2.0, 0.5, 4 )
y <- [Tstart = 60,T = 4.] sin( 2.0, 0.0, 4 ) 

T_rot := current_time() - t_rot

# decreasing field
x <- [T = 10] 2
x <- [T = 10] line( 2.0, -2.0 )
x <- [T = 10] 0

end := current_time()

x <- [Tstart = t_rot, T = T_rot] 0.2 * gate_from_file
y <- [Tstart = t_rot, T = T_rot] gate_from_file / 5


f := lambda x: x**2
# test gate from file
x <- [T = 20.,Tstart = end] 2 * gate_from_file / 5